import { useEffect, useState } from 'react';

const useFetchAllListFont = () => {
  const [fonts, setFonts] = useState([]);

  const onGetDataFont = async () => {};

  useEffect(() => {}, []);

  return {
    fonts,
  };
};

export default useFetchAllListFont;
